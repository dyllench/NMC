export type ProductCategory = {
  name: string;
  href: string;
  description: string;
  image: string;
};

export type FactoryImage = {
  label: string;
  image: string;
};

export type FeatureItem = {
  title: string;
  icon: string;
};
